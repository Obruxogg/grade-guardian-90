import { createFileRoute, useNavigate } from "@tanstack/react-router";import { useState } from "react";import { supabase } from "@/integrations/supabase/client";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Nova senha | Repertório"},{name:"description",content:"Defina uma nova senha para o Repertório."},{property:"og:title",content:"Nova senha | Repertório"},{property:"og:description",content:"Defina uma nova senha para o Repertório."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const navigate=useNavigate();const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm){setError("As senhas não coincidem.");return;}const{error:updateError}=await supabase.auth.updateUser({password});if(updateError){console.error("[Password update]",updateError);setError("Não foi possível atualizar sua senha.");return;}await navigate({to:"/login",replace:true})}return <main className="grid min-h-screen place-items-center px-5"><form className="w-full max-w-md space-y-4 rounded-lg border bg-card p-7" onSubmit={submit}><h1 className="font-display text-3xl font-semibold">Crie uma nova senha</h1><div className="space-y-2"><Label htmlFor="password">Nova senha</Label><Input id="password" type="password" minLength={12} value={password} onChange={(e)=>setPassword(e.target.value)} required className="h-12"/></div><div className="space-y-2"><Label htmlFor="confirm">Confirmar senha</Label><Input id="confirm" type="password" minLength={12} value={confirm} onChange={(e)=>setConfirm(e.target.value)} required className="h-12"/></div>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<Button className="h-12 w-full">SALVAR NOVA SENHA</Button></form></main>}