import { createFileRoute } from "@tanstack/react-router";import { RoleGate } from "@/components/auth/RoleGate";import { useAuth } from "@/auth/AuthContext";import { Button } from "@/components/ui/button";export const Route=createFileRoute("/aluno")({head:()=>({meta:[{title:"Área do aluno | Repertório"},{name:"description",content:"Área segura do aluno no Repertório."},{property:"og:title",content:"Área do aluno | Repertório"},{property:"og:description",content:"Área segura do aluno no Repertório."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const auth=useAuth();return <RoleGate allow={["student"]}><main className="min-h-screen p-8"><h1 className="font-display text-4xl font-semibold">Olá, {auth.profile?.full_name??"aluno"}</h1><p className="mt-3 text-muted-foreground">Sua área está protegida. As provas permanecem pausadas nesta etapa.</p><Button className="mt-6" onClick={()=>void auth.signOut()}>Sair</Button></main></RoleGate>}