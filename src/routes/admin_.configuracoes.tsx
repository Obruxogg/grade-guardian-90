import { createFileRoute } from "@tanstack/react-router";import { useServerFn } from "@tanstack/react-start";import { CheckCircle2 } from "lucide-react";import { useEffect,useState } from "react";import { RoleGate } from "@/components/auth/RoleGate";import { WorkspaceShell } from "@/components/layout/WorkspaceShell";import { getSystemHealth } from "@/lib/identity.functions";import { adminItems } from "./admin";
export const Route=createFileRoute("/admin_/configuracoes")({head:()=>({meta:[{title:"Configurações | Repertório"},{name:"description",content:"Status da fundação do sistema Repertório."},{property:"og:title",content:"Configurações | Repertório"},{property:"og:description",content:"Status da fundação do sistema Repertório."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const load=useServerFn(getSystemHealth);const[data,setData]=useState<Record<string,string>|null>(null);useEffect(()=>{load().then(setData).catch((e)=>console.error("[System health]",e))},[load]);const labels:Record<string,string>={backend:"Backend",database:"Banco",authentication:"Autenticação",profiles:"Profiles",roles:"Roles",storage:"Storage"};return <RoleGate allow={["admin"]}><WorkspaceShell area="Administração" items={adminItems}><h1 className="font-display text-4xl font-semibold">Configurações</h1><section className="mt-7 max-w-2xl rounded-lg border bg-card p-6"><h2 className="font-display text-2xl font-semibold">Status do Sistema</h2><div className="mt-5 divide-y">{Object.keys(labels).map((key)=><div key={key} className="flex items-center justify-between py-4"><span className="font-medium">{labels[key]}</span><span className="flex items-center gap-2 text-sm font-semibold text-primary"><CheckCircle2 className="size-4"/>{data?.[key]??"Verificando"}</span></div>)}</div></section></WorkspaceShell></RoleGate>}