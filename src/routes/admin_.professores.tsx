import { createFileRoute } from "@tanstack/react-router";import { useServerFn } from "@tanstack/react-start";import { Check, Copy, Loader2, UserPlus } from "lucide-react";import { useState } from "react";import { RoleGate } from "@/components/auth/RoleGate";import { WorkspaceShell } from "@/components/layout/WorkspaceShell";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { createTeacherAccess } from "@/lib/identity.functions";import { adminItems } from "./admin";
export const Route=createFileRoute("/admin_/professores")({head:()=>({meta:[{title:"Professores | Repertório"},{name:"description",content:"Cadastro seguro de professores e acessos."},{property:"og:title",content:"Professores | Repertório"},{property:"og:description",content:"Cadastro seguro de professores e acessos."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const create=useServerFn(createTeacherAccess);const[form,setForm]=useState({fullName:"",email:"",status:"active" as "active"|"archived"});const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const result=await create({data:form});setPassword(result.temporaryPassword);setForm({fullName:"",email:"",status:"active"});}catch(err){setError(err instanceof Error?err.message:"Não foi possível criar o professor.");}finally{setLoading(false)}}return <RoleGate allow={["admin"]}><WorkspaceShell area="Administração" items={adminItems}><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase text-muted-foreground">Equipe</p><h1 className="mt-2 font-display text-4xl font-semibold">Professores</h1></div></div><div className="mt-7 grid gap-6 lg:grid-cols-[minmax(0,1fr)_22rem]"><section className="rounded-lg border bg-card p-6"><div className="flex items-center gap-3"><UserPlus className="text-primary"/><h2 className="font-display text-2xl font-semibold">Novo professor</h2></div><form className="mt-6 space-y-4" onSubmit={submit}><div className="space-y-2"><Label htmlFor="teacherName">Nome</Label><Input id="teacherName" value={form.fullName} onChange={(e)=>setForm({...form,fullName:e.target.value})} required className="h-12"/></div><div className="space-y-2"><Label htmlFor="teacherEmail">E-mail</Label><Input id="teacherEmail" type="email" value={form.email} onChange={(e)=>setForm({...form,email:e.target.value})} required className="h-12"/></div><div className="space-y-2"><Label htmlFor="teacherStatus">Status</Label><select id="teacherStatus" value={form.status} onChange={(e)=>setForm({...form,status:e.target.value as "active"|"archived"})} className="h-12 w-full rounded-md border bg-background px-3"><option value="active">Ativo</option><option value="archived">Arquivado</option></select></div>{error&&<p role="alert" className="rounded-md bg-destructive/10 px-3 py-2 text-sm text-destructive">{error}</p>}<Button className="h-12" disabled={loading}>{loading?<Loader2 className="animate-spin"/>:<UserPlus/>} CRIAR ACESSO</Button></form></section><section className="rounded-lg border bg-card p-6"><h2 className="font-display text-xl font-semibold">Primeiro acesso</h2>{password?<div className="mt-4"><p className="flex items-center gap-2 text-sm font-medium text-primary"><Check/> Acesso criado</p><p className="mt-3 text-sm text-muted-foreground">Copie a senha temporária agora. Ela não será mostrada novamente.</p><div className="mt-3 flex gap-2"><Input readOnly value={password} aria-label="Senha temporária"/><Button type="button" size="icon" variant="outline" aria-label="Copiar senha temporária" onClick={()=>void navigator.clipboard.writeText(password)}><Copy/></Button></div><p className="mt-3 text-xs text-muted-foreground">O professor deve entrar em /login e usar “Esqueci minha senha” para definir uma senha pessoal.</p></div>:<p className="mt-4 text-sm text-muted-foreground">Após criar o acesso, a senha temporária exclusiva aparecerá aqui uma única vez.</p>}</section></div></WorkspaceShell></RoleGate>}