import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { MailCheck } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
export const Route=createFileRoute("/forgot-password")({head:()=>({meta:[{title:"Recuperar senha | Repertório"},{name:"description",content:"Recupere com segurança o acesso ao Repertório."},{property:"og:title",content:"Recuperar senha | Repertório"},{property:"og:description",content:"Recupere com segurança o acesso ao Repertório."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const[email,setEmail]=useState("");const[sent,setSent]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");const{error:resetError}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});if(resetError){console.error("[Password reset]",resetError);setError("Não foi possível enviar a recuperação agora.");return;}setSent(true)}return <main className="grid min-h-screen place-items-center px-5"><section className="w-full max-w-md rounded-lg border bg-card p-7">{sent?<><MailCheck className="size-11 text-primary"/><h1 className="mt-5 font-display text-3xl font-semibold">Confira seu e-mail</h1><p className="mt-2 text-muted-foreground">Se a conta existir, você receberá as instruções para criar uma nova senha.</p></>:<><h1 className="font-display text-3xl font-semibold">Esqueci minha senha</h1><p className="mt-2 text-muted-foreground">Informe o e-mail usado no acesso da equipe.</p><form className="mt-6 space-y-4" onSubmit={submit}><div className="space-y-2"><Label htmlFor="email">E-mail</Label><Input id="email" type="email" value={email} onChange={(e)=>setEmail(e.target.value)} required className="h-12"/></div>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<Button className="h-12 w-full">ENVIAR RECUPERAÇÃO</Button></form></>}<Button asChild variant="link" className="mt-4 px-0"><Link to="/login">Voltar ao login</Link></Button></section></main>}